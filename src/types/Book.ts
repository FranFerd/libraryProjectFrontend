export interface BookPreview{
    id: number,
    title: string,
    author: string,
    image: string
}

export interface BookDescription {
    author: string,
    description: string,
    id: number,
    image: string,
    pages: number,
    title: string,
    yearOfPublish: string
}
